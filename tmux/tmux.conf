###############################################
# Custom bindings
###############################################
# remap prefix to Control + a
set -g prefix C-a
unbind C-b

# Kill without prompting and re-gen layout
# bind x kill-pane \; select-layout
bind & kill-window

# Bind the last window command
bind C-b last-window

# Synchronize mode, send same command to all panes
bind a set-window-option synchronize-panes

# make copy mode like visual mode in vim
bind Escape copy-mode
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'Space' send -X halfpage-down
bind-key -T copy-mode-vi 'BSpace' send -X halfpage-up

# setup a modal 2-key bind for select-layout options
# example: <prefix> + V + 1 == tmux select-layout even-horizontal
bind V send-keys "$HOME/dotfiles/tmux/scripts/tmux-l.sh" \; send-keys "Enter"

###############################################
# Customizing default options
###############################################
# Vim shortcuts for help and copy modes
setw -g mode-keys vi

# Mouse works as expected
set-option -g mouse on

# Set the display panes timeout
set-option -g display-panes-time 4000
# Use base 1 numbering, 0 base is a big stretch
set -g base-index 1
setw -g pane-base-index 1

# monitoring
setw -g monitor-activity on
set -g visual-activity on

# don't rename windows automatically
set-option -g allow-rename off

# reduce esc delay in neovim
# see: https://github.com/neovim/neovim/wiki/FAQ#esc-in-tmux-or-gnu-screen-is-delayed
set -sg escape-time 10


######################
### DESIGN CHANGES ###
######################

#### COLOUR (Solarized light)

# default statusbar colors
set-option -g status-bg white #base2
set-option -g status-fg yellow #yellow
set-option -g status-attr default

# default window title colors
set-window-option -g window-status-fg brightyellow #base00
set-window-option -g window-status-bg default
#set-window-option -g window-status-attr dim

# active window title colors
set-window-option -g window-status-current-fg brightred #orange
set-window-option -g window-status-current-bg default
#set-window-option -g window-status-current-attr bright

# pane border
set-option -g pane-border-fg white #base2
set-option -g pane-active-border-fg brightcyan #base1

# message text
set-option -g message-bg white #base2
set-option -g message-fg brightred #orange

# pane number display
set-option -g display-panes-active-colour blue #blue
set-option -g display-panes-colour brightred #orange

# clock
set-window-option -g clock-mode-colour green #green
#### END SOLARIZED


## Status bar design
# status line
# set -g status-justify left

# # window status
# setw -g window-status-format " #F#I:#W#F "
# setw -g window-status-current-format " #F#I:#W#F "
# setw -g window-status-format "#[fg=magenta]#[bg=black] #I #[bg=cyan]#[fg=colour8] #W "
# setw -g window-status-current-format "#[bg=brightmagenta]#[fg=colour8] #I #[fg=colour8]#[bg=colour14] #W "
# # setw -g window-status-attr reverse

# # loud or quiet?
# set-option -g visual-activity off
# set-option -g visual-bell off
# set-option -g visual-silence off
# set-window-option -g monitor-activity off
# set-option -g bell-action none

# # The modes {
# setw -g mode-attr bold

# # The statusbar {
# set -g status-position bottom
# # set -g status-attr dim
# set -g status-left ''
# # set -g status-right '#{prefix_highlight} #[fg=colour15,bg=colour19,bold] #(gcalcli agenda --nocolor | head -2 | tail -1 | tr -s " " | cut -c -50) #[fg=colour15,bg=colour20,bold] %d/%m #[fg=colour15,bg=colour19,bold] %H:%M:%S '
# set -g status-right '#{prefix_highlight} #[fg=colour15,bg=colour20,bold] %d/%m #[fg=colour15,bg=colour19,bold] %H:%M:%S '
# set -g status-right-length 75
# set -g status-left-length 20

# setw -g window-status-current-format ' #I#[fg=colour15]:#[fg=colour15]#W#[fg=colour3]#F '
# setw -g window-status-format ' #I#[fg=colour15]:#[fg=colour15]#W#[fg=colour15]#F '



# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-pain-control'
set -g @plugin 'tmux-plugins/tmux-sessionist'
set -g @plugin 'tmux-plugins/tmux-yank'
# set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'thornycrackers/tmux-drawer'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
