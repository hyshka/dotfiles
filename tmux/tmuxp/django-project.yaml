session_name: "${PROJECT}"
start_directory: "~/Lift/${PROJECT}"
windows:
  - window_name: code
    focus: true
    panes:
      - shell_command:
        - nvim
        focus: true
  - window_name: serve/build
    layout: even-vertical
    panes:
      #- workon $(tmux display-message -p '#S')
      - blank
      - blank
      - blank
