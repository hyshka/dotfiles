session_name: "${PROJECT}"
start_directory: "/home/hyshka/Lift/${PROJECT}"
windows:
  - window_name: code
    panes:
      - shell_command:
        - nvim
        focus: true
      - shell_command:
        #- tmux resizep -t 2 -y 20; tmux resize-pane -Z -t 1; clear
        - tmux resizep -t 2 -y 20; clear
  - window_name: docker
    layout: even-vertical
    panes:
      #- workon $(tmux display-message -p '#S')
      - blank
      - blank
      - blank
